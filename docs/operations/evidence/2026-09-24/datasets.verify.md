# Adversarial verification: lens "appears-crawl-archive"

- **What was verified:** `docs/operations/evidence/2026-09-24/appears-crawl-archive.md` at commit `da1bd887cd6e993753ad87bdc99bfd77f69c85b1` on branch `work/site-visibility` of `mallan67/mayaallan`.
- **Verifier's time window (UTC):** 2026-09-24T18:39:43Z to 2026-09-24T18:51:13Z.
- **Live sources used:**
  - Wayback CDX, timemap and availability APIs (web.archive.org, archive.org)
  - Common Crawl index server (index.commoncrawl.org)
  - archive.today timemap (archive.ph)
  - Live GET/HEAD requests to www.mayaallan.com and the alias hosts
  - Vercel MCP `get_firewall_config`, scoped to project prj_CkwsvLxnWkKGJlyAA93lRVxAOQ9Y and team team_kZQh5NYLyrOKqffK0r9EXf4E
  - GitHub REST API (`gh api`) for commit and branch state
- **Not used:** repository source files, local files, scratch files and the Playwright browser. No forms were submitted and no login was attempted. No /api/cron, /api/admin or /api/indexnow requests were made.
- **Method:** each finding was re-run live and tested against these ways it could be wrong: a transient blip (2-3 retries), a wrong URL or query form, a measurement artefact, blocking or CAPTCHA mistaken for absence, and a wrong severity. A **positive control** was run on Common Crawl: `CC-MAIN-2026-39-index?url=archive.org` returned a real record at 18:48:27Z. That shows the index answers normally, so its "No Captures found" replies are real results and not outages.
- **How this file was saved:** in 4 appending commits, because a single shell command could not hold the whole file. Each commit fetched the live file from GitHub and appended one part. The last commit holds the complete file.

## Verdict table

| id | Verdict | Severity (orig -> verified) | Live re-check (URL / query, UTC) | Notes (refutation attempts) |
|---|---|---|---|---|
| ds-01 | confirmed | high -> **medium** | CDX `url=mayaallan.com&matchType=domain&limit=1000` @18:39:43Z: 93 rows, all 202601, first 20260130223929, last 20260131124458, only host www.mayaallan.com. Re-read @18:46:38Z and @18:48:03Z (matchType=host): same 93. CDX `from=202602` @18:40:47Z: `[]`. Timemap `/web/timemap/link/mayaallan.com` @18:40:08Z: last memento 20260131005213. Availability API @18:40:42Z (timestamp=20260924): closest 20260131005213; `www.mayaallan.com/blog`: `{}`. CDX `mayaallan.vercel.app` domain @18:40:45Z: `[]`. | The data is exact and was reproduced 3 times. The CDX returned data, so there is no exclusion or block. The CDX indexing lag is days, not 8 months. **Severity lowered:** a missing Wayback record does not by itself cost search visibility, and many small sites are archived only through manual Save Page Now. It is a symptom that supports ds-02, not a cause. "Fits almost no inbound links" is an inference; confirming it needs a backlink source (GSC Links report, Bing Webmaster backlinks), marked **needs outside-source check**. The solution is right in direction. Save Page Now is the only direct fix for the archive record itself. |
| ds-02 | confirmed | high -> high | collinfo @18:41:07Z. The 14 indexes CC-MAIN-2025-33 to CC-MAIN-2026-39 (crawl windows 2025-08-02 to 2026-09-17, read @18:51:12Z) were queried with `url=mayaallan.com&matchType=domain` between 18:41:26Z and 18:49:02Z. All 14 answered HTTP 404 `No Captures found`. The first parallel pass hit 503s and connection timeouts; those were re-run one at a time with curl `--retry 3` and every one ended as 404 "No Captures". `*.mayaallan.com` and `www.mayaallan.com/*` in 2026-39 and 2026-34 @18:50:00Z-18:50:03Z: No Captures. psilowire.com and psilocybinintegrationguide.com in 2026-39/34/30/25/04 @18:49:03Z-18:49:59Z: No Captures. One 504 on psilowire 2026-25 was retried to 404. | Positive control passed, so absence is not an outage artefact. Crawls that started after the first Wayback evidence of the site (2026-01-30): 2026-08, -12, -17, -21, -25, -30, -34, -39 = **8**, as the finding says. robots.txt allows CCBot live (18:47:35Z) and did in January (Wayback `20260130224105id_` robots.txt read @18:50:19Z: `User-Agent: *` / `Allow: /`). Nobody can see robots.txt for Feb-Aug, since nothing was archived then. Kept high because two independent public crawl datasets agree the site is not discoverable. That is the most direct third-party evidence behind "not seen anywhere". The solution (off-site links) targets the right cause, but results arrive only on the next CC crawls, about monthly. |
| ds-03 | confirmed | medium -> **low** | Wayback CDX domain: psilowire.com `[]` @18:45:49Z and @18:45:53Z, prefix form `[]` @18:45:56Z (first try timed out @18:44:27Z). psilocybinintegrationguide.com `[]` @18:45:06Z. Timemaps 200 with a 1-byte (empty) body @18:44:57Z and @18:45:26Z. Availability API `{}` @18:45:00Z and @18:45:30Z. archive.ph timemap @18:46:01Z-18:46:07Z: structured 404 "no Mementos" for apex and www of both, and also for mayaallan.com and www.mayaallan.com. HEAD @18:45:36Z-18:45:37Z: all 4 alias roots 308 to https://www.mayaallan.com/. | archive.ph returned its structured 404 text, not a CAPTCHA page, so the absence is genuine. Common Crawl also shows 0 (see ds-02). **Severity lowered:** redirect-only secondary domains with no footprint are expected, and the finding itself calls this "not an error". The solution is sound: link the canonical host everywhere and use the aliases offline. Extra detail: http://psilowire.com and http://psilocybinintegrationguide.com take 2 hops (308 to https apex, then 308 to www) @18:51:12Z-18:51:13Z. |
| ds-04 | confirmed (wording corrected) | medium -> **low** | Live sitemap GET @18:46:21Z: 38 `<url>`, 38 `<loc>`. Joined with CDX @18:46:38Z: 5 archived (/, /about, /contact, /legal, /books/psilocybin-integration-guide), **33 never archived**. January sitemap `20260130224104id_` @18:46:49Z: 10 locs. | The counts reproduce exactly. **Wording is wrong:** the January sitemap already listed /books, /events, /media and /media/Mushroom-Healing. None of those 4 were archived either, so the gap covers January-era pages too, not only pages "added since January". The phrase "any third-party crawl record" is too strong: only Wayback and Common Crawl were checked, and search-engine crawl state is unknown (**needs outside-source check**: GSC or Bing Webmaster). **Severity lowered:** this is the same symptom as ds-01 at URL level and has no direct ranking effect. **The solution only partly fixes it.** Off-site links raise crawl odds but do not guarantee that the Internet Archive captures each of the 33 URLs. If archive coverage itself matters, it needs an owner-run Save Page Now with "save outlinks". |
| ds-05 | confirmed | low -> low | Live sitemap @18:46:21Z: 8 `<lastmod>`, 0 `<changefreq>`, 0 `<priority>`, 84 `xhtml:link` alternates. The 30 URLs without lastmod include /, /about, /books, /blog, /faq, /glossary and all /es /pt /de /fr /he pages and their /about pages. | Reproduced exactly. Caveats: Google ignores `changefreq` and `priority`, so missing them is **not a defect** (the solution correctly does not ask for them). All 5 blog lastmods are the same `2026-04-19T00:00:00.000Z`. Owner or content should confirm that is the real date. Accurate lastmod helps only after a crawler has the sitemap. With zero discovery (ds-02) this is marginal, and whether the sitemap was submitted to GSC or Bing needs an **outside-source check**. |
| ds-06 | confirmed (facts), impact overstated | low -> **info** | CDX `www.mayaallan.com/admin` prefix @18:46:50Z: 20260130230146 /admin/ 308, 20260130234335 /admin 307, 20260130234335 /admin/login 200. HEAD /admin/login @18:47:05Z: 200, `Cache-Control: private, no-cache, no-store`, no X-Robots-Tag. /admin 307 to /admin/login and /admin/ 308 to /admin @18:47:09Z. **GET /admin/login @18:47:09Z: `<meta name="robots" content="noindex, nofollow, nocache"/>`** | The original evidence **missed that the page already sends meta robots noindex,nofollow**. robots.txt `Disallow: /admin/` also blocks compliant crawlers from /admin/login. The proposed `X-Robots-Tag` fix is therefore **redundant for indexing**. The only real residue is the public Wayback copy, which only an owner-filed exclusion request removes. There is no visibility impact, so it is downgraded to info. Minor detail: /admin/login's canonical is https://www.mayaallan.com, which is harmless on a noindex page. |
| ds-07 | confirmed (facts), interpretation uncertain | info -> info | CDX host @18:48:03Z and @18:48:17Z: 7 non-asset paths (6 HTML 200 plus /index.xml), 47 `_next/static` (3 x 404), 16 `_next/image`, 15 probes (.well-known/*, ads.txt 404, feeds), 5 admin/api, 3 robots/sitemap/favicon. 2 `dpl=` ids: dpl_DBeh47tbTSnvNP1NJM5VZWvRan2E and dpl_2JkpWDg7aKTJdPGAxufZ7ecchVN2. By hour: 2026013022=30, 23=25, 2026013100=18, 01=10, 02=3, 03=1, 04=1, 07=3, 09=1, 12=1. | "One burst, not recurring" holds. **"Single browser-driven capture" does not hold as stated.** The captures span about 14 hours in 10 distinct hours, with 2 homepage captures (22:40 and 00:52). The crawler also fetched /admin/ and /api/, which are the robots.txt Disallow paths, plus a list of standard /.well-known probes. That pattern points to an automated archive crawler run, possibly mixed with browser-rendered assets. Who triggered it cannot be known from public data. It is historical only, and "no solution needed" is correct. |