# Adversarial verification: appears-index-presence (2026-09-24)

> Status: this file was assembled in 4 sequential commits because of the shell's command-length limit. The final commit is the complete verdict.

- **Lens verified:** appears-index-presence. Evidence file `docs/operations/evidence/2026-09-24/appears-index-presence.md` at commit `23f8fd6a3b8b03b46f887c731fd5ef36d0c9d4f8` (confirmed with `gh api repos/mallan67/mayaallan/commits/23f8fd6…` at 2026-09-24T18:42:14Z).
- **Branch head read at start:** `work/site-visibility` = `22613ee45358390cc08c43c872d02bc4c0e37dbb` (gh api, 2026-09-24T18:42:14Z).
- **UTC window of live checks:** 2026-09-24T18:42:14Z to __END__.
- **Sources used (all live, read-only):** curl GET to www.mayaallan.com and its alternate hosts (redirects not followed); Bing HTML via curl and Bing RSS (`format=rss`); WebFetch (Bing, Google, Brave, DuckDuckGo); curl to Google, Brave, DuckDuckGo html and lite, Mojeek, Startpage, Yahoo and Ecosia; the Common Crawl index API; the Wayback CDX and availability APIs; public book and profile pages about Maya Allan (Amazon, Google Books, Barnes & Noble, Instagram, Goodreads), GET only; Vercel MCP `get_firewall_config` (project prj_CkwsvLxnWkKGJlyAA93lRVxAOQ9Y, team team_kZQh5NYLyrOKqffK0r9EXf4E); `gh api` for repo metadata, issues, PR metadata, workflows, runs and search/issues.
- **Not used:** repository source files, any local file or scratch directory, Playwright, logins, forms, `/api/cron/*`, `/api/admin/*`, `/api/indexnow/*`.
- **WebSearch was unavailable for this pass.** The first call (about 18:45:20Z) returned "this session has used its web search budget (200 of 200 WebSearch calls)". So no claim that rests only on the WebSearch engine could be re-run. Those claims are marked uncertain or scoped to Bing.

## 1. Measurement artefacts found (these change how the lens evidence can be read)

| # | Artefact | Live observation (URL / call, UTC) |
|---|---|---|
| M1 | **Bing title and topic queries from this environment fail a positive control, so they cannot prove absence.** The post `/blog/inherited-beliefs-grandmother-marriage` is in Bing's index: it appears in `site:mayaallan.com`. But Bing did not return it for its own exact title or for its key words. Quoted or multi-word queries collapse to their first word. | Bing RSS `How your grandmother's fear shows up in your marriage` top result m.youtube.com, no own URL (18:52:44Z). `grandmother fear marriage inherited beliefs` and `inherited beliefs grandmother marriage` returned merriam-webster (18:52:45Z). Curl HTML `"The difference between an affirmation and an integration"` returned Wikipedia "The" (18:44:12Z); WebFetch of the same query also returned "The" results (about 18:44:50Z). WebFetch `"What does ego dissolution feel like…"` returned WhatsApp (about 18:44:55Z). Every `Maya Allan <topic>` query on Bing RSS returned Autodesk Maya (18:44:40Z–18:44:45Z). |
| M2 | **Bing ignores `site:` when a term is added.** | `site:mayaallan.com belief inquiry`, `nervous system reset`, `integration journal`, `ego dissolution`, `faq` and `glossary` returned unrelated sites (curl 18:44:14Z–18:44:18Z). `site:mayaallan.com blog` and `site:mayaallan.com scenarios` did the same (RSS 18:44:28Z–18:44:29Z). |
| M3 | **Bing's `site:` listing is incomplete.** `/books/psilocybin-integration-guide` is #1 on Bing for "psilocybin integration guide" but never appears in the `site:mayaallan.com` list. The list always shows the same 7 URLs, and `first=8` and `first=11` return those same 7. Bing's count, "About 36 results", is an estimate and sits close to the 38 URLs in the sitemap. The visible list is therefore a lower bound on what Bing has indexed. | `site:mayaallan.com` curl 18:43:13Z, 18:43:14Z, 18:43:33Z; `first=8` and `first=11` at 18:43:34Z; RSS 18:44:28Z. Book page #1: RSS 18:44:46Z and 18:47:41Z. |
| M4 | **Every non-Bing engine blocked automated reads.** | Google, Chrome UA: JavaScript page with 0 results, twice (18:45:54Z). Google, Lynx UA: "update your browser", twice (18:45:55Z). Google via WebFetch: "trouble accessing" (about 18:45:40Z). Brave: HTTP 429 twice by curl (18:45:26Z) and by WebFetch. DuckDuckGo html: HTTP 202 challenge (18:45:35Z), CAPTCHA via WebFetch. DuckDuckGo lite: 202 challenge (18:53:06Z). Mojeek: "Captcha" (18:45:36Z). Startpage: Anubis challenge (18:46:14Z). Yahoo: 307 to a bot page (18:46:06Z). Ecosia: 403 firewall (18:46:06Z). Goodreads search: 202 (18:53:42Z). |

## 2. Verdicts

| id | verdict | severity (lens → judged) | Live re-check (UTC) | Why |
|---|---|---|---|---|
| idx-01 | **uncertain** | high → high | Sitemap: 38 URLs (18:42:25Z). All 38 return 200 with `index, follow`, no X-Robots-Tag and a self canonical (18:51:30Z–18:52:03Z). Bing `site:` shows 7 URLs, and the book page adds an 8th (see M3). | The positive part reproduces: 8 URLs are visible on Bing. The absence part does not. The Bing title and topic probes fail a positive control (M1, M2), the `site:` list is demonstrably incomplete (M3), WebSearch could not be re-run and every other engine blocked (M4). "8 of 38" is a lower bound, not the index footprint. One point supports the claim: the two pages in PR #45, a "controlled two-page indexing test" merged 2026-09-05T12:16:43Z (gh api 18:54:12Z), are `/blog/psilocybin-integration-research` and `/scenarios/ego-dissolution`, and neither is in Bing's `site:` list 19 days later. The true coverage is only visible in GSC and Bing Webmaster Tools (Bing WMT). |
| idx-02 | **confirmed** | high → high | Google by curl: 4 of 4 blocked (18:45:54Z–18:45:55Z). WebFetch blocked (about 18:45:40Z). Startpage, which serves Google results, blocked (18:46:14Z). | Reproduced. Google index presence cannot be read by automation, so it stays UNVERIFIED. |
| idx-03 | **uncertain** | medium → low | Bing RSS "psilocybin integration guide": #1 is www.mayaallan.com/books/psilocybin-integration-guide, ahead of Amazon (#2 and #4), Google Books (#3) and B&N (#6) (18:44:46Z, 18:47:41Z). | The Bing half reproduces. The WebSearch half, where the page was missing and Amazon ranked #1–#3, could not be re-run (budget exhausted), and no other engine could be read (M4). One unmeasurable engine where the page is missing, while Bing ranks it #1, does not justify medium. |
| idx-04 | **refuted** | medium → low | Live head tags (18:46:59Z) against Bing snippets (18:43:33Z). `/contact` has `og:description` and `twitter:description` = "Get in touch for press, collaborations, or reader inquiries.", exactly the Bing snippet. The Bing snippet for `/blog`, "Research-backed essays on belief systems, nervous system reg…", is live hero text on `/blog`. `/books` has `og:title` "Maya Allan", and its `og:description` and `twitter:description` are the homepage text Bing shows. | The snippets are not stale: Bing is quoting current page content (Open Graph and body text), not an old crawl. So recrawling or IndexNow will not change them. Only the title separators remain unexplained: Bing shows "About - Maya Allan" and "Books - Maya Allan", while the live titles use a vertical bar ("About &#124; Maya Allan"). That could be a title from before PR #43 ("doubled titles" fix, merged 2026-09-05T10:33:26Z) or a Bing rewrite; Bing does show "Contact &#124; Maya Allan" as written. That part is uncertain. The real defect is inconsistent page metadata (see N1). |
| idx-05 | **confirmed** | low → low | Sitemap (18:42:25Z): 8 of 38 URLs have lastmod; all 5 posts read `2026-04-19T00:00:00.000Z`; the static and localized URLs have none. JSON-LD and `article:modified_time` read at 18:54:27Z. | Reproduced, and worse than stated. `/blog/psilocybin-integration-research` declares `dateModified` 2026-09-05 in both JSON-LD and `article:modified_time`, but the sitemap lastmod is 2026-04-19. The lastmod is wrong on one of the two PR #45 indexing-test pages. `/scenarios/ego-dissolution` matches (2026-09-05). It contributes, but it is not the root cause. |