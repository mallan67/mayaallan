# Adversarial verification: real-browser-console lens (2026-09-24)

- **Lens verified:** real-browser-console. The audit being checked is `docs/operations/evidence/2026-09-24/audit-browser-console.md` (commit `65c6e4ee1463da7f88527313503a06690521a7ce` on `work/site-visibility`; that commit was re-read through `gh api` at 18:49:59Z).
- **Time window (UTC):** 2026-09-24T18:49:59Z to 19:01:11Z. The save time is in the commit.
- **Live sources used:**
  - HTTP GET and HEAD (curl, desktop Chrome and iPhone Safari user agents) against https://www.mayaallan.com and the project's other domains.
  - Live production JS and CSS chunks, all carrying `?dpl=dpl_5ug8W5ASgWuKHpxSYScqtAH1q9Q4`. Home HTML at 18:53:35Z showed that ID 46 times, so production is still that deployment.
  - Vercel MCP scoped to `prj_CkwsvLxnWkKGJlyAA93lRVxAOQ9Y` / `team_kZQh5NYLyrOKqffK0r9EXf4E`: `get_project`, `count_pageviews`, `count_events`, `aggregate_pageviews`, `get_deployment`.
  - GitHub live metadata through `gh api`: PR #57, its check runs and the compare view.
- **Method limitation (read this first):** I did **not** use the Playwright MCP. Finding ui-17 says it writes files into the local repository folder, and the owner's rule forbids local-file activity. Browser behaviour was re-checked against the **live production JavaScript** served by the site (deployed chunks, not repository files) and the live server HTML. **Nothing was clicked, POSTed or submitted.** Pixel positions, console output and POST responses were **not** re-measured; each row says where this applies.
- **Local files:** none were written or read. Captures were held only in shell variables.

## Verdicts

| id | verdict | severity (audit → verified) | live evidence now (URL / tool, UTC) | does the proposed solution fix it? |
|---|---|---|---|---|
| ui-01 | **confirmed** | high → **high** | Live chunk `/_next/static/chunks/312c5210c9c35dc5.js?dpl=dpl_5ug8…` (GET 18:53:45Z, 18:54:00Z) contains `function P(){return"accepted"!==(0,S.useConsent)()?null:(0,t.jsx)(f,{})}` and `function k(){return"accepted"!==(0,S.useConsent)()?null:(0,t.jsx)(E,{})}`. **Both** the Vercel Analytics component (`scriptSrc:"9de18cd67c0a6252/script.js"`) **and** the first-party visitor setup (`fetch("/api/marketing/visitor",{method:"POST"…})`) render only when `localStorage.mayaallan_consent_v1 === "accepted"`. PR #57: `state=open merged=false mergeable=true mergeable_state=clean`, checks `gates=success` and `Vercel=success`, `behind_by=0`, `updated=2026-09-07T03:49:42Z` (gh api 18:50:00Z, 18:58:18Z). | **Only partly.** Merging PR #57 is necessary but **not sufficient**. The PR's own description says "Vercel Web Analytics is disabled at the project level, so page views are discarded", so without ui-02 the cookieless page views are still thrown away. The PR needs no more work (clean, green, 0 behind main); only the owner's merge is missing. Ship it with ui-02 and ui-15. |
| ui-02 | **confirmed** | medium → **high** | Three independent live reads, all returning `400 web_analytics_not_enabled`: `count_pageviews` (~18:53Z), `count_events` (~18:58Z) and `aggregate_pageviews` by day (~18:58Z). GET `/9de18cd67c0a6252/script.js` and `/_vercel/insights/script.js` both return 200 `application/javascript`, 4469 B (18:53:35Z): the script loads, but the project stores nothing. The audit's POST `/view` → 200 was **not** re-tested (POST forbidden). | **Yes.** Enabling Web Analytics for project `mayaallan` (Vercel → project → Analytics → Enable) makes Vercel store page views. To check it, `count_pageviews` must stop returning `web_analytics_not_enabled`. Until PR #57 merges, only visitors who click "Accept all" are counted. Severity raised because this one owner-side switch is why even consenting visitors leave **zero** stored page views, and it is the prerequisite for ui-01's fix. |
| ui-03 | **confirmed** (DOM order; pixels not re-measured) | medium → **medium** | Server HTML of `/books/psilocybin-integration-guide` (18:55:03Z, 18:55:24Z). Headings in order: h1 Psilocybin Integration Guide → About This Book → After your journey → Related reading → Available Formats → Buy Direct → Buy from Retailers. **619 visible words** come before the first "Buy … with PayPal" button and 689 before the first retailer link. `<main>` has no `order-*`, `col-start` or `row-start` classes (only `md:sticky` and `grid-cols-*`), so the text column shows content in DOM order. | **Yes.** Moving the Formats / Buy / Retailers block above "About This Book", or into the sticky cover column, puts it on the first screen. |
| ui-05 | **confirmed** | medium → **medium** | `/events`: 200, main text 15 words ("No events are currently scheduled…"), checked 18:50:22Z and 18:59:08Z. `/media`: 200, main 9 words, 1 image, **no href** to `/media/Mushroom-Healing` (share links only), checked 18:50:23Z and 18:59:09Z. A scan of all 38 sitemap URLs (18:58:47Z–18:59:08Z) found no page except the item itself linking to `/media/Mushroom-Healing`. That page is a back link, an image and a title, with a 19-character meta description. `/scenarios` links exactly 1 scenario (`/scenarios/ego-dissolution`) while its text advertises "40 real scenarios". | **Yes.** Fastest: hide or noindex Events and Media until they have content, and link the media card to its page. Highest value: more scenario pages (the book has 40), each targeting a real search question. Medium stays, because thin pages do not explain why the site is unseen. |
| ui-04 | **confirmed** | low → **low** | Server HTML at 18:50:10Z (desktop user agent) and 18:59:48Z (iPhone user agent) contains `href="https://www.paypal.com/signout"` "Sign out of PayPal", inside the notice "Using a shared computer? Make sure you're signed into your own PayPal account before purchasing.", text size `text-[11px]`. Live chunk `fc986f8dd4b371e4.js` (18:54:35Z) shows it is rendered conditionally (`a&&(…)`) in `PaymentButtons`, and the condition is currently on. The 97x14 px tap size was not re-measured. | **Yes.** Show it only after a PayPal error, or turn off that prop. |
| ui-06 | **confirmed**, with a correction | low → **low** | A scan of all 28 English sitemap URLs (18:57:49Z–18:58:07Z) plus the home header and footer (18:50:37Z) found **zero** links to `/es /pt /de /fr /he`. `/he` has `<html lang="he" dir="rtl">` but its header reads "Home Books Belief Inquiry … Contact" in English (18:50:52Z), and `/es` is the same. **Correction:** search engines can see the translations: `sitemap.xml` declares **84 `xhtml:link` hreflang alternates** (18:57:49Z), though the pages carry no hreflang in their HTML. | **Yes, for human visitors.** A language switcher plus translated menus fixes it. Search engines already find the pages through the sitemap. |
| ui-07 | **confirmed**, with a correction | low → **low** | Home header hrefs (18:50:37Z): `/ /books /belief-inquiry /nervous-system-reset /integration-reflection /media /events /about /contact`. None of the 17 footer hrefs is `/practices`. "Practices" in the footer is only an `<h4>` heading (18:51:01Z). A scan of all sitemap pages (18:51:01Z–18:51:23Z) found links to `/practices` only on `/practices` itself and on `/methods`. **Correction:** the page is one click deeper through /methods, not unreachable. | **Yes.** Smallest fix: make the existing footer heading "Practices" a link to `/practices`. |
| ui-08 | **confirmed** | low → **low** | `/about` (18:51:35Z) and `/contact` (18:51:37Z): no external links apart from share buttons, no `mailto:`, no email-like text. JSON-LD `sameAs` is **only** `https://www.instagram.com/maya.allan66/`, and it is not shown on the page. | **Yes.** Also list the Amazon author page, Goodreads and other profiles in JSON-LD `sameAs`, so search engines tie the site to the author. A visible email address is optional, since a contact form exists. |
| ui-09 | **uncertain** (one part confirmed) | low → **low** | **Confirmed:** home HTML (18:55:37Z, 18:56:04Z) has `<link rel="preload" as="image">` for the hero with `imageSizes="(max-width: 768px) 0px, 45vw"`. Its smallest srcset candidate is 384w, inside a `hidden md:block` wrapper, so phones fetch a w=384 hero they never show. **Not reproduced:** the cover being downloaded twice. The preload and `<img>` carry **identical** srcset and sizes on `/` and on the book page, so a 384→640 mismatch would need a viewport or pixel-ratio change after load (such as `browser_resize`), which is a likely measurement artefact. The "hero also at w=640" is probably the lazy "Maya Allan" portrait: the **same blob file** (`1771580739166-1.jpg`, `srcSet` 256 1x / 640 2x), shown as a different, visible image. | **Partly.** Drop `priority`/preload from the hidden hero, or render it only at md and up; that fixes the confirmed part. The "cover twice" part needs a clean browser run at a fixed viewport before any code change. |
| ui-10 | **confirmed** | low → **low** (borderline info) | Two fetches at 18:52:41Z (`/this-page-does-not-exist-xyz1`, `…xyz2`): 404 with `<meta name="robots" content="noindex">` **plus** `robots "index, follow"` **plus** `googlebot "index, follow, …"`. The 404 page also has **`<link rel="canonical" href="https://www.mayaallan.com">`** and the home page's title. No `X-Robots-Tag` header. | **Yes.** Also remove the canonical from the not-found page. Google acts on the 404 status, so this is cosmetic. |
| ui-11 | **confirmed** (CSS); iOS zoom not reproduced | low → **low** | `/contact` name, email and message fields use `text-sm`. Live CSS `ac184285d4e67ed8.css` has `.text-sm{font-size:.875rem}` and no root font-size override, so they are **14 px at every width** (18:56:26Z, 18:56:38Z). The textareas on `/belief-inquiry`, `/nervous-system-reset` and `/integration-reflection` use `text-[0.95rem] sm:text-base`, which is **15.2 px below 640 px**. The viewport allows zoom (`maximum-scale=5`). | **Yes.** `text-base` (16 px) on mobile inputs stops iOS Safari zooming on focus. |
| ui-13 | **confirmed** (mechanism, from live code) | low → **info** | Chunk `312c5210c9c35dc5.js` (18:54:11Z): `function E(){…if("1"===sessionStorage.getItem("ma_bootstrapped"))return;sessionStorage.setItem(x,"1")…}` runs once per tab. The **Reject** handler is only `a("rejected")`, a localStorage write. None of the 14 loaded chunks deletes a cookie (no `max-age=0`, no 1970 expiry; checked 18:54:35Z). So "Reject analytics" does **not** delete the ma_* cookies, which answers the audit's open caveat: a visitor who changes their mind keeps the cookies and stays identified. The bug shows only if cookies vanish mid-tab through manual clearing. | **The fix works but is worth little today.** Coupling: fixing V-01 (Reject deletes cookies) makes this bug real, so clear `ma_bootstrapped` on reject in the same change. |
| ui-14 | **confirmed** | low → **low** | Book page HTML at 18:50:10Z and 18:59:48Z. Barnes & Noble: `;jsessionid=E0C61A17…`. AbeBooks: `clickid=…&cm_mmc=aff-_-ir-_-353196-_-77798&afn_sr=impact&ref_=aff_ir_353196_77798`. Bookshop: `source=IndieBound&ref=https%3A%2F%2Fwww.google.com%2F`. Bokus: `srsltid=…`. Each appears twice (HTML and RSC payload). **Needs outside-source check:** whether the B&N jsessionid URL still resolves, and who owns Impact affiliate 353196 (third-party domains are out of scope). | **Yes.** Use clean retailer URLs or the author's own affiliate links. Low, because with almost no traffic the revenue effect is negligible. |
| ui-15 | **confirmed**, with a correction | low → **low** (becomes **medium** once PR #57 merges) | `/privacy` (18:51:48Z, 18:51:58Z), "Last updated: May 20, 2026". **Correction:** it **does** name Vercel twice, but only as "hosts the website and stores public assets in Vercel Blob". It never mentions "Web Analytics" or the book_viewed / no-consent event, and says analytics events are stored in Supabase. | **Yes**, and it must ship **with** PR #57, which makes Web Analytics run without consent. |
| ui-16 | **confirmed**, with a correction | low → **low** | The `<title>` of `/blog/psilocybin-integration-research` is **97 characters decoded**. The audit's 102 counted the raw HTML entity `&#x27;` as 6 characters (raw 102 / decoded 97, re-read at 18:52:10Z and 18:59:48Z). **Under-scoped:** 17 of the 38 sitemap titles exceed 60 characters (18:52:10Z–18:52:28Z), e.g. `/practices` 106, `/es` 104, `/fr` 98, `/blog/curiosity-over-judgment-ifs` 96, `/pt` 92, `/nervous-system-reset` 91, `/blog/affirmations-vs-integration` 90. | **Only if applied site-wide.** Shortening this one post leaves 16 others too long. Consider dropping the " - Maya Allan" suffix on long titles. Low, because Google often rewrites long titles anyway. |
| ui-12 | **confirmed** (from live code) | info → **info** | Chunk `fc986f8dd4b371e4.js` (18:54:35Z): `fetch("/api/marketing/event",{method:"POST",…eventName:"book_viewed",path:"/books/…",properties:{book_id,slug,title,direct_sale_enabled,ebook_price}})`. The only guard is the sessionStorage key `ma_book_viewed:<bookId>`, and the chunk contains **no** `useConsent` reference. Not re-POSTed (forbidden). **Nuance:** as a same-origin fetch it carries any ma_* cookies from an earlier Accept, which Reject does not delete (V-01). "No cookie" holds only for visitors who never accepted. | **Yes.** Keep it and disclose it (ui-15). |
| ui-17 | **uncertain** | info → **info** | Checking it would mean listing a local folder (`Desktop\mayaallan\.playwright-mcp`), which the owner's rule forbids. I deliberately did not use the Playwright MCP, so no further files were added. | **Owner decision:** delete the folder, and/or run the Playwright MCP with an output directory outside any repository copy. Until then, do not use it for these audits. |

## New observations from this verification

| id | severity | observation | live evidence (UTC) | proposed fix |
|---|---|---|---|---|
| V-01 | low | **"Reject analytics" does not remove cookies set by an earlier "Accept all".** `ma_visitor_id` (Max-Age 63072000, 2 years), `ma_first_touch` and `ma_last_touch` (2 years) and `ma_session_id` (1800 s) stay in the browser and go out with every same-origin request, including the no-consent `book_viewed` POST. | Chunk `312c5210c9c35dc5.js` (18:54:11Z, 18:54:35Z): the reject handler is `onClick:()=>a("rejected")`, which only sets localStorage `mayaallan_consent_v1` and dispatches `mayaallan:consent-changed`. None of the 14 chunks loaded by `/`, the book page and `/practices` expires a cookie. | On reject, expire every `ma_*` cookie and clear `sessionStorage.ma_bootstrapped`, then re-test ui-13. |
| V-02 | info | **Every evidence commit to `work/site-visibility` triggers a Vercel preview build.** | `get_project` (~18:53Z): latestDeployment `dpl_46fniR82Hod31pHZfD5bSGbttrzb`, INITIALIZING. `get_deployment`: `githubCommitRef=work/site-visibility`, commit `5ce968b7…`, target null (preview), created 18:53:20Z, READY 18:54:21Z. Production is unaffected. | Harmless, but it uses build minutes. If unwanted, the owner can add an Ignored Build Step for `work/*` branches. |

## "Works" spot-checks

| item | holds? | live re-check (UTC) |
|---|---|---|
| Every sitemap page and locale page loads (200) | **yes** | The sitemap now has **38** URLs (the audit said 30). All 38 return 200, including the 10 locale URLs (18:52:10Z–18:52:28Z). robots.txt and llms.txt return 200 (18:56:55Z). |
| Secondary domains redirect to www | **yes** | 18:56:50Z: mayaallan.com, psilowire.com and psilocybinintegrationguide.com (with and without www) and mayaallan.vercel.app all return **308** to `https://www.mayaallan.com/`. The path is kept (`/books` → `www…/books`, 18:57:14Z). `http://www` → 308 to https. The two `-mallan` aliases → 302 to Vercel SSO. |
| Canonicals, robots tags and JSON-LD are correct | **yes** | All 38 sitemap URLs have a self-referencing canonical, `index, follow`, and JSON-LD that parses (18:57:14Z–18:57:32Z). The 404 page is the exception (ui-10). |
| Header, footer and mobile-menu links work | **yes** (header and footer; mobile menu not re-run) | Header: 9 distinct hrefs (the audit's 10 likely counted the logo's "/" twice). Footer: 17. All return 200 (18:56:55Z). |
| No cart, checkout or shop pages | **yes** | `/cart /checkout /shop /store /buy` all return 404 (18:56:53Z). |
| No images missing alt text | **yes** (server HTML) | The 38 pages hold only 8 `<img>` tags in total. None lacks `alt`; only the home hero uses `alt=""` (19:00:50Z–19:01:11Z). Broken-image (naturalWidth) checks were not re-run. |
| Pages respond fast | **yes** (curl, not Navigation Timing) | Time to first byte, including TLS, was 0.19–0.33 s for 19 URLs, with `/scenarios` at 0.77 s (18:56:55Z). |
| Cookie banner: nothing tracks before a choice | **yes** (from live code) | Both analytics components are gated on `"accepted"` (ui-01). **Caveat:** "Reject leaves 0 cookies" is true only for a first-time visitor (V-01). Button sizes were not re-measured. |
| Tool pages and forms render correctly | **partly** | The contact spam-trap field is inside `aria-hidden="true"` with `left-[-9999px]` and has `tabindex="-1"` (18:56:38Z). The disabled Send button, the journal radio button and the 28 FAQ anchors were **not** re-run. |
| No JavaScript errors in a real browser | **UNVERIFIED** | No browser run (see Method limitation). Not refuted. |
| No failed network requests | **UNVERIFIED** for subresources | Every document request returns 200. Chunk and image requests were not listed in a browser. |
| No sideways scrolling | **UNVERIFIED** | Needs layout measurement in a browser. |

## Not done / unverified (explicit)

- **Needs a browser run** (blocked by the local-file rule, not by the site): pixel positions (ui-03, and the ui-04 tap size), console messages (the "no JS errors" item and the ui-09 preload warning), horizontal overflow, and the mobile-menu aria behaviour.
- **Forbidden POST requests:** the 200 responses from `/api/marketing/visitor`, `/api/marketing/event` and `/9de18cd67c0a6252/view` were not re-tested.
- **Needs an outside-source check:** whether the Barnes & Noble `jsessionid` link resolves, who owns Impact affiliate `353196`, and actual iOS Safari zoom on a real device.
- **ui-17:** needs the owner to look at the local folder. The agent must not look.